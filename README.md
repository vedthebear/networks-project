# networks-project
Collection of code + data for the Math 168 project.

# develop-moltbook-v1 workflow
1. Run fetch_data.py with your own Moltbook API key in a .env file to generate required datasets for analysis
2. Run subsequent "analyze_*.py" files to view figures and quanitative measurements
